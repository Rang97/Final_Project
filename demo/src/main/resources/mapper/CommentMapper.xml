<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.domain.post.repository.CommentMapper">
    <select id="findVisibleCommentsByPostId" resultType="com.example.demo.domain.post.dto.CommentResponse">        SELECT
        c.comment_id AS commentId,
        c.post_id    AS postId,
        c.content    AS content,
        c.created_at AS createdAt,
        s.saju_animal_name AS writerAnimalName
        FROM comment c
        JOIN saju s ON c.writer_id = s.user_id
        <where>
            c.post_id = #{postId}
            <if test="blockerId != null">
                AND c.writer_id NOT IN (
                SELECT blocked_id FROM block WHERE blocker_id = #{blockerId}
                )
            </if>
        </where>
        ORDER BY c.created_at ASC
    </select>
</mapper>